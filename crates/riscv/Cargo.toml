cargo-features = ["per-package-target"]

[package]
name = "riscv"
version = "0.1.0"
description = "RISC-V Architecture specific components."
authors = ["Justus K <justus.k@protonmail.com>"]
edition = "2018"
forced-target = "riscv64gc-unknown-none-elf"

[dependencies]
